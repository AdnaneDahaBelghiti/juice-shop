name: Generate SBOM

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  generate-sbom:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20.18.0'

      - name: Install latest CycloneDX tool from GitHub
        run: npm install --save-dev https://github.com/CycloneDX/cyclonedx-node-module

      - name: Install project dependencies
        run: npm install

      - name: Generate SBOM in JSON format
        run: npx cyclonedx-bom --output-format json --output-file sbom.json

      - name: Upload SBOM artifact
        uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: sbom.json
